"const { createClient } = require('@supabase/supabase-js'); const fs = require('fs'); require('dotenv').config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const sql = fs.readFileSync('migrations/014_hr_ops_tables.sql', 'utf8'); const { data, error } = await supabase.rpc('execute_sql', { sql: sql }); console.log('data:', data, 'error:', error); } run();"  
